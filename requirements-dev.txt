# Host-side test tools. The gateway itself is tested through its Docker API,
# so this intentionally does not install the production image's Python 3.12
# pins (which may not have wheels for the host Python version).
pytest>=8.3,<9
httpx>=0.27,<1
python-jose>=3.3,<4
fastapi>=0.111,<1
